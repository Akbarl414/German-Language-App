{
  "name": "german-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run validate && vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-content.js",
    "test": "node --test src"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "vite": "^5.4.11"
  }
}
